db-programming-assignment
=========================

Installation
------------

1. Clone the git repository
2. Install dependencies (i.e., `pip install -r dependencies.txt`)
3. Run script (i.e., `python test.py`)
